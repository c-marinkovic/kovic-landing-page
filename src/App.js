import React from "react";
import './App.css'; // Vinculamos el archivo de estilos

function App() {
  return (
    <div className="App">
      <section id="intro"> {/* Sección de introducción */} </section>
      <section id="esfera"> {/* Sección de la esfera psicopompo */} </section>
      <section id="portales"> {/* Sección de portafolio / bio / contacto */} </section>
      <section id="footer"> {/* Sección de pie de página */} </section>
    </div>
  );
}

export default App;
